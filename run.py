# This is where we config and run the app

from src.app import app

app.run()

# [DONE] Home + signup + sending email=> H => (100%)
#
# [DONE] After signup page =>  H => (100%)
#
# [DONE] Forgot pwd => H => (100%)
#
# Login => H => 70%
#
# Update profile => H => 0%
#
# Booking confirm page(changing time???/ default time/ sending email) =>  H
